================
Model components
================


A very basic battery model will contain a positive electrode, a negative electrode and an electrolyte.
We can also add current collectors to our models by setting include\ :sub:`current`\ \ :sub:`collectors`\ = true.
