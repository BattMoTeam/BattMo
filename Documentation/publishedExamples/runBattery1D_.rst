We consider a one-dimensional model.

We run thermo coupled simulation on a 1D model

The domain is split in three regions :

* two electrodes
* electrolyte

We use orgLiPF6 solvent

.. image:: ../battmologo.png
   :scale: 20%
