{
  "$id": "file://./ThermalComponent.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Thermal model",
  "type" : "object",
  "properties" : {
    "ThermalModel" : {
      "useWetProperties" : {
        "type" : "boolean",
        "description" : "If set to true, we use wet properties, which means that the thermal properties are measured with the electrolyte. In this case, it corresponds to the effective thermal properties. This property is NOT yet SUPPORTED. It could be set to false or ignored"},
      "externalHeatTransferCoefficient": {
        "$ref": "PhysicalQuantity.schema.json",
        "description": "the heat transfer coefficient between the external surface and surroundings"
      },
      "externalHeatTransferCoefficientTopFaces" :  {
        "$ref": "PhysicalQuantity.schema.json",
        "description": "the heat transfer coefficient between the top surfaces and surroundings"
      },
      "externalHeatTransferCoefficientSideFaces" : {
        "$ref": "PhysicalQuantity.schema.json",
        "description": "the heat transfer coefficient between the side surfaces and surroundings"
      },
      "externalTemperature": {
        "$ref": "PhysicalQuantity.schema.json",
        "description": "the temperature of the surroundings"
      },
      "effectiveThermalConductivity" : {
        "$ref": "PhysicalQuantity.schema.json",
        "description": "the effective thermal conductivity of the domain; this value is usually not given but computed from the other components"
      },
      "effectiveHeatCapacity" : {
        "$ref": "PhysicalQuantity.schema.json",
        "description": "the effective heat capacity of the domain; this value is usually not given but computed from the other components"
      }
    },
    "NegativeElectrode" : {
      "type" : "object",
      "properties" : {
        "Coating" : {
          "$ref" : "#/$defs/thermalParameters"},
        "CurrentCollector" : {
          "$ref" : "#/$defs/thermalParameters"}}},

    "PositiveElectrode" : {
      "type" : "object",
      "properties" : {
        "Coating" : {"$ref" : "#/$defs/thermalParameters"},
        "CurrentCollector" : {"$ref" : "#/$defs/thermalParameters"}}},

    "Electrolyte" : {
      "type" : "object",
      "properties" : { "Separator" : {"type" : "object",
                                      "$ref" : "#/$defs/thermalParameters"}}}},
  "anyOf" : [
    {"properties" : {
      "ThermalModel" : {
        "properties" : {
          "useWetProperties" : {"const" : false}}},
      "Electrolyte" : {
        "type" : "object",
        "$ref" : "#/$defs/thermalParameters"}}},
    {"properties" : {
      "ThermalModel" : {
        "properties" : {
          "useWetProperties" : {"const" : true}}}}}
  ],

  "$defs" : {
    "thermalParameters" : {
      "type": "object",
      "properties" : {
        "thermalConductivity": {"type" : "number"},
        "density" : {"type" : "number"},
        "specificHeatCapacity": {"type" : "number"}}}}

}

